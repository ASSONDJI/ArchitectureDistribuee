package com.example.Order_Service.services;

import com.example.Order_Service.dto.product.ProductItemDTO;
import org.springframework.stereotype.Component;
import org.springframework.stereotype.Service;

@Component
public class ProductClientStimulation {
   /* public ProductItemDTO getProductById(Long id) {
        ProductItemDTO product = new ProductItemDTO();
        product.setProductId(id);
        product.setName("Produit simulé " + id);
        product.setQuantity(1);
        product.setUnitPrice(10.0);  // prix fictif
        return product;
    }*/
}
